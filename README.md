# Daily Todo
Run `start.bat`; open `http://127.0.0.1:8765/index.html`. UI: `index.html`/`styles.css`/`app.js`; server/reminders: `todo_server.py`; scripts: `*.ps1`,`*.bat`. Data is `todo-data/`: private, never commit. Test `py -m unittest discover -s tests -v`. Package with `make-exe-package.ps1` or `make-package.ps1`. Use PR branches; do not commit caches/logs/secrets/artifacts.
