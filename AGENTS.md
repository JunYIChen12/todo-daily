# AGENTS.md
Preserve CRUD, search/filter, day switch, recurrence, `/api/store`, reminders, Windows toast/fallback, install/start/package scripts, tests, CI, `.gitignore`. Never read/commit/migrate/delete/rewrite `todo-data/`; exclude caches/logs/artifacts/secrets/temp files. Before edits identify touched files/functions and risk. Keep scope small, avoid duplicates/refactors, run `py -m unittest discover -s tests -v`, and run/dry-run touched packaging scripts or document skips. Short PR branches; keep `main` working.
